eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
    status-page-url-path: /actuator/info #eureka注册中心的url link
    health-check-url-path: /actuator/health #健康检查的url
    hostname: localhost
    status-page-url: http://localhost:${server.port}/swagger-ui.html
  client:
    registryFetchIntervalSeconds: 5
    register-with-eureka: true #是否注册到注册中心
    fetch-registry: true #是否从注册中心拉取注册服务列表
    service-url:
      defaultZone: http://localhost:10010/eureka/ #注册中心地址
#---------------------------------------------------------------------
spring:
  application:
    name: employee-server
  cloud:
    #    consul:
    #      host: 192.168.1.80
    #      port: 8500
    #      discovery:
    #        enabled: true
    #        register: true
    #        healthCheckInterval: 5s
    #        health-check-timeout: 1s
    #        prefer-ip-address: true
    #        health-check-path: /actuator/health
    config:
      name: employee-server,db-config,druid-config,monitor-config,hystrix-config
      failFast: true
      discovery:
        enabled: true
        serviceId: config-server
      retry:
        initial-interval: 1000
        multiplier: 1.2
        max-interval: 2000
        max-attempts: 6